diff --git a/diff-corpus/scenarios/neutral-09-redundant-defaults-added/before.rdl b/diff-corpus/scenarios/neutral-09-redundant-defaults-added/after.rdl
index dbe5351..a7e0987 100644
--- a/diff-corpus/scenarios/neutral-09-redundant-defaults-added/before.rdl
+++ b/diff-corpus/scenarios/neutral-09-redundant-defaults-added/after.rdl
@@ -1,4 +1,11 @@
 addrmap soc {
-    reg { field { sw = rw; hw = r; reset = 0x0; } en[0:0]; } ctrl @ 0x0;
-    reg { field { sw = r; hw = w; } busy[0:0]; } status @ 0x4;
+    reg {
+        regwidth = 32;
+        accesswidth = 32;
+        field { sw = rw; hw = r; reset = 0x0; } en[0:0];
+    } ctrl @ 0x0;
+    reg {
+        regwidth = 32;
+        field { sw = r; hw = w; } busy[0:0];
+    } status @ 0x4;
 };
