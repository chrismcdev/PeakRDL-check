# PROOF

```text
Claim 1: Supported
Claim 2: Supported
```

Generated by `scripts/build_proof.py` from raw result files on
2026-07-13 15:35 +1000. No number in this document was typed
by hand; regenerate with `./scripts/build-proof-report`.

## Claims (exact wording)

**Claim 1.** RegReview makes 800,000-register specifications immediately
browsable, incrementally rebuildable in under one minute, and materially
faster and more efficient to generate than PeakRDL-html.

**Claim 2.** RegReview identifies and explains breaking register-interface
changes that an ordinary textual Git diff cannot classify reliably.

## Environment

- Hardware: Apple M4 Pro, 14 cores, 24 GiB RAM
- OS: macOS-26.5.1-arm64-arm-64bit
- Python: 3.12.12 · systemrdl-compiler 1.32.2 · peakrdl-html 2.12.2 · regreview 0.1.0
- Cache condition: every run is a fresh process; the OS file cache is warm
  for both tools equally (see docs/baseline-methodology.md).
- Fixtures: deterministic seeded generator, elaborated register counts
  verified by `regreview-fixture verify` (manifests in fixtures/manifests/).
  The 800k fixture is the mixed realistic profile (register arrays + 40%
  repeated block types), `--seed 12345`.

## Claim 1 — measured criteria

| Criterion | Result | Evidence |
|---|---|---|
| 800k fixture builds successfully (3+ runs) | **PASS** | 3/3 runs ok, median 284.9s |
| index generation from elaborated model < 60 s | **PASS** | 10.2s (traverse+write+index medians) |
| incremental local block change < 60 s (3 runs) | **PASS** | 32.9s (200/800 units), 32.2s (200/800 units), 32.6s (200/800 units) |
| existing-index server ready < 2 s | **PASS** | median 105 ms |
| first usable viewer response < 2 s | **PASS** | median 107 ms |
| exactLookup p95 < 50 ms | **PASS** | p95 0.602 ms |
| registerDetail p95 < 100 ms | **PASS** | p95 0.389 ms |
| childrenPage p95 < 100 ms | **PASS** | p95 1.16 ms |
| search p95 < 200 ms | **PASS** | p95 10.399 ms |
| addressRange p95 < 200 ms | **PASS** | p95 7.174 ms |
| no response contains the full hierarchy (bounded payloads) | **PASS** | largest observed API response 94 KB (85,151 folded rows / 800,000 registers would be tens of MB) |
| peak memory and output size recorded | **PASS** | peak RSS 10.4 GB, index 338 MB |
| materially fewer filesystem objects than PeakRDL-html | **PASS** | regreview 800k: 1 file(s); peakrdl-html 800k: 85,216 files |
| cold raw-source build < 5 min (initial-release target) | **PASS** | median 285s |

### Cold build matrix (medians over successful runs)

| Fixture | Registers | Tool | Wall (median) | Peak RSS | Files | Output | Runs OK |
|---|---|---|---|---|---|---|---|
| 1k | 1,000 | peakrdl-html | 0.7 s | 0.08 GB | 186 | 9 MB | 7/7 |
| 1k | 1,000 | regreview | 0.2 s | 0.07 GB | 1 | 1 MB | 7/7 |
| 10k | 10,000 | peakrdl-html | 4.7 s | 0.39 GB | 1,136 | 21 MB | 3/3 |
| 10k | 10,000 | regreview | 2.2 s | 0.38 GB | 1 | 4 MB | 6/6 |
| uniq10k | 10,000 | peakrdl-html | 61.5 s | 3.98 GB | 10,078 | 136 MB | 3/3 |
| uniq10k | 10,000 | regreview | 21.8 s | 3.85 GB | 1 | 49 MB | 3/3 |
| 100k | 100,000 | peakrdl-html | 56.2 s | 3.21 GB | 10,603 | 143 MB | 3/3 |
| 100k | 100,000 | regreview | 18.7 s | 3.32 GB | 1 | 42 MB | 6/6 |
| 400k | 400,000 | peakrdl-html | 435.4 s | 7.15 GB | 42,613 | 552 MB | 3/3 |
| 400k | 400,000 | regreview | 105.8 s | 7.68 GB | 1 | 169 MB | 3/3 |
| 800k | 800,000 | peakrdl-html | 1389.6 s | 10.27 GB | 85,216 | 1098 MB | 3/3 |
| 800k | 800,000 | regreview | 284.9 s | 10.40 GB | 1 | 338 MB | 3/3 |

PeakRDL-html at 800k registers: median 1390 s, 85,216 files, peak RSS 10.3 GB (3/3 ok).

### Cold 800k build, stage breakdown (regreview)

| Stage | Median |
|---|---|
| Parse (ANTLR, systemrdl-compiler) | 181.9 s |
| Elaborate | 10.2 s |
| Canonical traversal | 7.3 s |
| Index row writes | 2.7 s |
| Secondary index creation | 0.2 s |

The permitted-claims discipline: parse dominates the cold build; the claim
"builds a queryable index from an elaborated 800,000-register model in
10 seconds" refers to
traversal+write+index only. The full raw-source cold build is reported
separately above and is NOT claimed to be under one minute.

### Incremental rebuilds (one-line edit in one block-type file)

- run 1: 32.9 s (hash 0.04 s, standalone re-elaboration 32.1 s, splice 0.62 s; 200 units rebuilt, 600 reused)
- run 2: 32.2 s (hash 0.04 s, standalone re-elaboration 31.4 s, splice 0.71 s; 200 units rebuilt, 600 reused)
- run 3: 32.6 s (hash 0.04 s, standalone re-elaboration 31.3 s, splice 1.28 s; 200 units rebuilt, 600 reused)

Incremental output is proven equivalent to a clean rebuild by
`scripts/verify_incremental_equivalence.py` (also enforced in
tests/test_incremental.py).

### Interactive use (existing index; p50/p95 over 200 randomized requests)

| Index | DB | Ready | First usable | Lookup p50/p95 | Children p50/p95 | Search p50/p95 | Range p50/p95 |
|---|---|---|---|---|---|---|---|
| 100k | 42 MB | 72 ms | 74 ms | 0.21 / 0.31 ms | 0.85 / 0.91 ms | 1.62 / 1.67 ms | 1.80 / 2.10 ms |
| 400k | 169 MB | 104 ms | 106 ms | 0.34 / 0.66 ms | 0.87 / 1.06 ms | 4.40 / 5.20 ms | 2.76 / 4.21 ms |
| 800k | 338 MB | 105 ms | 107 ms | 0.36 / 0.60 ms | 0.91 / 1.16 ms | 8.15 / 10.40 ms | 4.12 / 7.17 ms |

## Claim 2 — measured criteria

| Criterion | Result | Evidence |
|---|---|---|
| 100% detection of curated breaking scenarios | **PASS** | 12/12 pass |
| 100% suppression of formatting-only scenarios | **PASS** | 10/10 pass (incl. a 203-line refactor diff -> 0 semantic changes) |
| all corpus scenarios pass | **PASS** | 47/47 |
| corpus has 40+ scenarios | **PASS** | 47 |
| mutation recall | **PASS** | 1.0 over 160 semantic trials |
| mutation precision | **PASS** | 1.0 (0 FP) |
| neutral mutation suppression | **PASS** | 1.0 over 80 trials |

Corpus: 47 scenarios under diff-corpus/scenarios/, each with
before/after sources, the raw `git diff`, the semantic diff (JSON+Markdown)
and machine-checked expectations. Highlights:

- difficult-10-large-refactor-no-change: 203-line git diff, **0** semantic
  changes.
- difficult-05-parameter-change-many-instances: 12-line git diff (one
  parameter), 10 elaborated reset changes reported with explanations.
- difficult-08-ambiguous-rename: two identical candidates — the engine
  reports MATCH-UNCERTAIN and keeps the removals; it never silently assumes
  a rename.
- breaking-10/11 (address/field overlap): systemrdl-compiler itself rejects
  overlapping definitions at elaboration; RegReview reports
  SPEC-COMPILE-FAILED (breaking), which is the honest observable outcome.

Git diff comparison stance: git operates on text, by design; these results
do not show git "failing" — they show that hardware-interface severity is
not derivable from textual difference size in either direction.

## Reproduction

```bash
./scripts/bootstrap
./scripts/generate-fixtures          # deterministic, seeded, verified
./scripts/test                       # unit tests + 47-scenario corpus
.venv/bin/python scripts/mutation_tests.py 240
bash benchmarks/scripts/run_full_matrix.sh   # full build/query matrix (hours)
.venv/bin/python benchmarks/scripts/bench_incremental.py  # 3 incremental runs
./scripts/build-proof-report         # regenerate reports + this file
```

Raw evidence: benchmarks/raw-results/ (one JSON per run, including failures
and timeouts), benchmarks/results.json/csv, benchmarks/report.html,
diff-corpus/results.json, diff-corpus/report.html.

## Limitations

See docs/known-limitations.md. Notable: cold builds are dominated by
upstream ANTLR parsing; the incremental unit is the defining file; the
GitHub Action was exercised through a local simulation of the same driver
(scripts/simulate_pr_workflow.sh), not a hosted runner.
